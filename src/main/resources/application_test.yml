server:
  port: 8080
  servlet:
    context-path: /api

spring:
  config:
    activate:
      on-profile: "test"
  datasource:
    url: jdbc:mysql://localhost:3306/fc_management
    username: root
    password: Tinh0501@@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    default-property-inclusion: always
media:
  saver:
    active: local
    local:
      absolute-path: ""
      host: http://localhost:${server.port}/${server.servlet.context-path}/resources
logging:
  ignores-path:
    - "/api/resources/**"